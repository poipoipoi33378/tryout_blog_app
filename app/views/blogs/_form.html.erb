<%= form_for (blog) do |f| %>
  <%= render 'shared/form_error',object: blog %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>